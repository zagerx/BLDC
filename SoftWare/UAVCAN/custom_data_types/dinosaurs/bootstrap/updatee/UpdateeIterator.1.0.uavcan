# This service allows the caller to discover the names of all updatee available on the server
# by iterating the index field from zero until an empty name is returned.
#
# The ordering of the updatee shall remain constant while the server is running.

uint8 index

@sealed

---

UpdateeDescribe.1.0 updatee

@sealed

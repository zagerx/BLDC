#only driver work in velocity mode, node need to transmit this message

uavcan.time.SynchronizedTimestamp.1.0 timestamp

# The motion distance of the wheel per unit time
uavcan.si.unit.velocity.Scalar.1.0[<=16] current_velocity

# The motion distance of the wheel as yet
uavcan.si.unit.length.Scalar.1.0[<=16] odometry

@sealed

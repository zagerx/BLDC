# A command call used to instruct a node, typically the PDB,
# to enter or exit low-power state. And keep the state for
# the ${duration} second if state = 1

# 0: normal state
# 1: hibernation state
uint8 state

# unit: second
int16 delay
int32 duration

@sealed

---

# 0: OK
int32 result
@sealed
